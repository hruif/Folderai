<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  PARENT entitlements for the Mac App Store build of FolderAI (the main app).
  __TEAMID__ is substituted with your Apple Team ID by scripts/sign-mas.sh.
  Proven necessary by the sandbox spike + the product design:
    app-sandbox            — required for the MAS.
    cs.allow-jit           — V8 needs it. (Do NOT add allow-unsigned-executable-memory:
                             it is banned for App Store apps.)
    application-groups      — Electron's main-process Mach-port rendezvous needs this or
                             it aborts at launch (verified in the spike).
    files.user-selected.read-write + bookmarks.app-scope — the folder-grant flow:
                             user-picked folders + persisted security-scoped bookmarks.
    network.client          — first-run model download (gguf is data, App-Store-legal).
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.app-sandbox</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.application-groups</key>
  <array>
    <string>__TEAMID__.com.folderai.app</string>
  </array>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <key>com.apple.security.files.bookmarks.app-scope</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
